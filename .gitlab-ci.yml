# Author: Kalidas, Janardhanan
# Updated: 2026-06-29

### use pipeline template (temporary local, later from gitlab)
include:
  - local: 'template.yml'

variables:
  ZCLI_VERSION: "1.0.0-beta.56"
  NODE_VERSION: "25.9.0"
  APPROVE_FOR_PROJECT: "fps_skc_pe"
  ### Set this temporary during pipelines debug for prevent actual deploy/rollback in PROD
  DRY_RUN: "true"
  ### Set these vars to dummy values for bypass check and do not break deployed theme.
  ### Set them to actual values in the Gitlab project/group CI/CD variables then.
  ZD_SUBDOMAIN: "my.subdomain"
  ZD_EMAIL: "john.doe@gmail.com"
  ZD_API_TOKEN: "1234567890"
